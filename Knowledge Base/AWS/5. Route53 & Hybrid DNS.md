---
tags:
  - aws/networking
  - review
status: completed
---
# Route53, Hybrid DNS & Direct Connect

## 📖 Core Concepts

> [!NOTE]
> **Big Picture First — What problem does each service solve?**

```mermaid
flowchart LR
    P1["❌ VPN over internet<br/>is slow & unpredictable"]
    P2["❌ Need DNS for<br/>public & private apps"]
    P3["❌ AWS & on-prem<br/>can't resolve each other"]

    S1["✅ Direct Connect<br/>Private dedicated wire to AWS"]
    S2["✅ Route 53<br/>Managed DNS + registrar"]
    S3["✅ Hybrid DNS<br/>Endpoints that bridge both worlds"]

    P1 --> S1
    P2 --> S2
    P3 --> S3

    classDef problem fill:#C62828,stroke:#B71C1C,color:#fff
    classDef solution fill:#2E7D32,stroke:#1B5E20,color:#fff
    class P1,P2,P3 problem
    class S1,S2,S3 solution
```

---

### 🔌 Part 1 — AWS Direct Connect (DX)

#### What is it?
A **physical, private cable** between your data center and an AWS DX location (a colocation facility). Traffic never touches the public internet.

> 🚂 **VPN over internet** = driving on a public highway. Fast most of the time, but traffic jams happen.
> 🛤️ **Direct Connect** = your own private railway track. Reserved, predictable, and consistent.

#### When to use Direct Connect

```mermaid
flowchart LR
    USE["Use Direct Connect when..."]
    USE --> A["📦 Syncing terabytes<br/>of data regularly<br/>(backups, DR replication)"]
    USE --> B["⚡ Need guaranteed<br/>low latency<br/>for sensitive apps"]
    USE --> C["💰 Reduce data<br/>egress costs<br/>(cheaper than internet DTO)"]
    USE --> D["🔒 Compliance requires<br/>traffic to NEVER touch<br/>the public internet"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef node fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class USE root
    class A,B,C,D node
```

#### Connection Types — Dedicated vs. Hosted

```mermaid
flowchart LR
    DX["DX Connection Types"]
    DX --> DED["<b>Dedicated</b><br/>Your own physical port<br/>1G · 10G · 100G<br/>Direct from AWS<br/>Longer lead time"]
    DX --> HOST["<b>Hosted</b><br/>Shared partner port<br/>50 Mbps → 10G<br/>Via AWS DX Partner<br/>Faster provisioning"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef left fill:#7B68EE,stroke:#5A4BA0,color:#fff
    classDef right fill:#50C878,stroke:#2F7A4F,color:#fff
    class DX root
    class DED left
    class HOST right
```

#### Virtual Interfaces (VIFs) — The 3 Lanes on Your Track

Once your physical link is live, you divide it into **Virtual Interfaces**. Think of each VIF as a separate lane with a different destination:

```mermaid
flowchart TD
    DC["🏢 Your Data Center"]
    DXL["DX Location<br/>(Colocation Facility)"]
    DC -->|"Physical cable<br/>1G / 10G / 100G"| DXL

    DXL --> PVIF["<b>Private VIF</b><br/>→ Virtual Private Gateway<br/>Reaches private IPs<br/>in ONE VPC"]
    DXL --> TVIF["<b>Transit VIF</b><br/>→ Transit Gateway<br/>Reaches private IPs<br/>in MANY VPCs"]
    DXL --> PUBVIF["<b>Public VIF</b><br/>→ AWS Public Endpoints<br/>S3 · DynamoDB · Route53<br/>(no internet)"]

    classDef dc fill:#E2A63F,stroke:#8A6A2E,color:#fff
    classDef dx fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef vif fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class DC dc
    class DXL dx
    class PVIF,TVIF,PUBVIF vif
```

> [!TIP]
> **Memory trick:** Private = **one VPC**. Transit = **many VPCs**. Public = **AWS public services** (still private wire, not the internet).

#### Direct Connect Gateway (DXGW) — Going Multi-Region

**Problem:** A VIF only reaches a TGW/VGW in **one region**. What if you have VPCs in `us-east-1` AND `eu-west-1`?

```mermaid
flowchart LR
    DC["🏢 Data Center"]
    DXLOC["DX Location"]
    DXGW["🌐 DX Gateway<br/>(Global Hub)"]

    TGW_US["TGW us-east-1<br/>(US VPCs)"]
    TGW_EU["TGW eu-west-1<br/>(EU VPCs)"]
    VPC1["VPC-A"]
    VPC2["VPC-B"]
    VPC3["VPC-C"]
    VPC4["VPC-D"]

    DC -->|"Transit VIF"| DXLOC --> DXGW
    DXGW --> TGW_US
    DXGW --> TGW_EU
    TGW_US --> VPC1
    TGW_US --> VPC2
    TGW_EU --> VPC3
    TGW_EU --> VPC4

    classDef dc fill:#E2A63F,stroke:#8A6A2E,color:#fff
    classDef hub fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef tgw fill:#7B68EE,stroke:#5A4BA0,color:#fff
    classDef vpc fill:#50C878,stroke:#2F7A4F,color:#fff
    class DC dc
    class DXGW,DXLOC hub
    class TGW_US,TGW_EU tgw
    class VPC1,VPC2,VPC3,VPC4 vpc
```

> [!IMPORTANT]
> DXGW supports **Private VIF** and **Transit VIF** only — not Public VIFs. A Transit VIF must attach to DXGW first; you **cannot** wire it directly to a TGW.

#### Is Direct Connect Encrypted?

> [!WARNING]
> **No. Direct Connect is NOT encrypted by default.** It's private (no internet), but unencrypted. Physical tap at the DX location = readable traffic.

```mermaid
flowchart LR
    DX["DX Link<br/>(unencrypted by default)"]
    DX --> Q{"Need<br/>encryption?"}
    Q -->|"No"| OK["Traffic flows as-is<br/>Private but plain text"]
    Q -->|"Yes — Layer 2<br/>Dedicated 10G/100G only"| MAC["MACsec<br/>Hardware-level encryption<br/>on the physical link"]
    Q -->|"Yes — Layer 3<br/>Works on any VIF"| IPSEC["IPsec VPN<br/>Site-to-Site VPN tunnel<br/>running over DX"]

    classDef root fill:#C62828,stroke:#B71C1C,color:#fff
    classDef ok fill:#2E7D32,stroke:#1B5E20,color:#fff
    classDef enc fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class DX root
    class OK ok
    class MAC,IPSEC enc
```

#### High Availability — BGP Path Control

```mermaid
flowchart TB
    subgraph OnPrem["🏢 On-Premises Routers"]
        R1["Router A<br/>(Primary Link)<br/>Local Pref = 200"]
        R2["Router B<br/>(Secondary Link)<br/>Local Pref = 100"]
    end
    subgraph AWS["☁️ AWS"]
        VGW["Virtual Private Gateway"]
    end

    R1 -->|"Preferred outbound path<br/>(higher Local Preference)"| VGW
    R2 -.->|"Standby outbound path"| VGW

    VGW -->|"Preferred inbound path<br/>(shorter AS PATH)"| R1
    VGW -.->|"Standby inbound path<br/>(prepended AS PATH = looks longer)"| R2

    classDef primary fill:#2E7D32,stroke:#1B5E20,color:#fff
    classDef secondary fill:#7B68EE,stroke:#5A4BA0,color:#fff
    classDef aws fill:#4A90E2,stroke:#2E5C8A,color:#fff
    class R1 primary
    class R2 secondary
    class VGW aws
```

> **On-Prem → AWS:** Use **BGP Local Preference** (higher = preferred path) on your router.
> **AWS → On-Prem:** Use **BGP AS PATH Prepending** (shorter path = AWS prefers it).

---

### 🌐 Part 2 — Route 53 (DNS)

#### What is DNS — One Sentence
DNS is a phone book: you look up a name (`api.example.com`) and get back an IP address (`52.10.1.5`) that your computer can connect to.

#### Route 53's 3 Jobs

```mermaid
flowchart LR
    R53(("Route 53"))
    R53 --> REG["📋 Domain Registration<br/>Buy yourcompany.com<br/>directly from AWS"]
    R53 --> DNS["🌐 DNS Hosting<br/>Host A · AAAA · CNAME<br/>Alias · MX · TXT records"]
    R53 --> HC["💓 Health Checking<br/>Monitor endpoints<br/>Reroute on failure"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef node fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class R53 root
    class REG,DNS,HC node
```

#### Hosted Zones — Public vs. Private

```mermaid
flowchart LR
    HZ["Hosted Zone"]
    HZ --> PUB["<b>Public</b><br/>Resolvable from internet<br/>Use case: public website/API<br/>e.g. api.mycompany.com → ALB"]
    HZ --> PRIV["<b>Private</b><br/>Resolvable inside VPC only<br/>Use case: internal DBs/services<br/>e.g. db.internal → RDS IP"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef pub fill:#50C878,stroke:#2F7A4F,color:#fff
    classDef priv fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class HZ root
    class PUB pub
    class PRIV priv
```

#### Alias Record vs. CNAME ⭐

```mermaid
flowchart LR
    Q["Pointing a DNS name<br/>to something else?"]
    Q --> AWS_RES{"Pointing to<br/>an AWS resource?"}
    AWS_RES -->|"Yes"| ALIAS["✅ Use ALIAS Record<br/>Free queries<br/>Works at zone apex<br/>ALB · CF · S3 · API GW"]
    AWS_RES -->|"No — external name<br/>e.g. 3rd party CDN"| CNAME["CNAME Record<br/>Billed per query<br/>Cannot use at root domain"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef good fill:#2E7D32,stroke:#1B5E20,color:#fff
    classDef warn fill:#E2A63F,stroke:#8A6A2E,color:#fff
    class Q root
    class ALIAS good
    class CNAME warn
```

#### Routing Policies — Decision Guide

```mermaid
flowchart TD
    START["User sends DNS query to Route 53"]
    START --> Q1{"Multiple records<br/>exist?"}
    Q1 -->|"No"| SIMPLE["Simple Policy<br/>Return the single record"]
    Q1 -->|"Yes"| Q2{"What's the<br/>routing goal?"}

    Q2 -->|"Split by percentage"| W["Weighted<br/>e.g. 90% prod / 10% canary"]
    Q2 -->|"Fastest region for user"| L["Latency-based<br/>Route to lowest-latency region"]
    Q2 -->|"Failover / DR"| F["Failover<br/>Primary healthy? → send there<br/>Primary down? → backup"]
    Q2 -->|"User's country matters"| G["Geolocation<br/>EU users → EU servers"]
    Q2 -->|"Distance + custom bias"| GP["Geoproximity<br/>Fine-tune cross-region shifts"]
    Q2 -->|"Basic load spread"| MV["Multi-value Answer<br/>Return up to 8 healthy IPs<br/>⚠️ NOT a replacement for ELB"]

    classDef start fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef policy fill:#7B68EE,stroke:#5A4BA0,color:#fff
    classDef simple fill:#50C878,stroke:#2F7A4F,color:#fff
    class START start
    class SIMPLE simple
    class W,L,F,G,GP,MV policy
```

---

### 🔗 Part 3 — Hybrid DNS (The Bridge Between DNS Worlds)

#### The Problem — Two Isolated DNS Worlds

```mermaid
flowchart LR
    subgraph ONPREM["🏢 On-Premises DNS World"]
        OD["AD DNS Server"]
        ON1["db.corp.internal"]
        ON2["mail.corp.local"]
        OD --- ON1
        OD --- ON2
    end
    subgraph AWSVPC["☁️ AWS DNS World"]
        R53R["Route53 Resolver<br/>(10.0.0.2)"]
        AW1["api.internal"]
        AW2["rds.prod.internal"]
        R53R --- AW1
        R53R --- AW2
    end

    ONPREM x--x|"❌ Can't see each other"| AWSVPC

    style ONPREM fill:#3E2723,stroke:#BF360C,color:#fff
    style AWSVPC fill:#0D47A1,stroke:#1565C0,color:#fff
```

**Problem 1:** EC2 can't resolve `db.corp.internal` — it only lives in on-prem DNS.
**Problem 2:** On-prem servers can't resolve `api.internal` — it only lives in Route53.

**Hybrid DNS solves both** by creating ENI-backed bridges (endpoints) that forward queries across DX or VPN.

#### Bridge 1 — Inbound Endpoint (On-Prem → AWS)

> On-prem wants to resolve an AWS private name like `rds.prod.internal`

```mermaid
sequenceDiagram
    participant APP as 🖥️ On-Prem App Server
    participant ODNS as 🏢 On-Prem DNS Server
    participant IN as 🔵 Inbound Endpoint (ENI)
    participant R53 as Route53 Resolver
    participant PHZ as Private Hosted Zone

    APP->>ODNS: What is rds.prod.internal?
    ODNS->>IN: Forward query (over DX/VPN)
    IN->>R53: Query to VPC resolver
    R53->>PHZ: Lookup record
    PHZ-->>R53: 10.0.5.22
    R53-->>IN: 10.0.5.22
    IN-->>ODNS: 10.0.5.22
    ODNS-->>APP: 10.0.5.22 ✅
```

#### Bridge 2 — Outbound Endpoint (AWS → On-Prem)

> An EC2 instance wants to resolve an on-prem name like `db.corp.internal`

```mermaid
sequenceDiagram
    participant EC2 as ☁️ EC2 Instance
    participant R53 as Route53 Resolver
    participant RULE as Resolver Rule (corp.internal)
    participant OUT as 🟣 Outbound Endpoint (ENI)
    participant ODNS as 🏢 On-Prem DNS Server

    EC2->>R53: What is db.corp.internal?
    R53->>RULE: Check Resolver Rules...
    RULE-->>R53: Forward Rule matched!<br/>Send to 192.168.10.5
    R53->>OUT: Forward query (over DX/VPN)
    OUT->>ODNS: What is db.corp.internal?
    ODNS-->>OUT: 192.168.1.50
    OUT-->>R53: 192.168.1.50
    R53-->>EC2: 192.168.1.50 ✅
```

#### Centralized Hybrid DNS at Scale

```mermaid
flowchart LR
    subgraph HUB["🔧 Networking Account (Hub)"]
        IN["Inbound Endpoint"]
        OUT["Outbound Endpoint"]
        RULES["Resolver Rules<br/>(Forward corp.internal → on-prem)"]
    end
    subgraph RAM["AWS RAM"]
        SHARE["Shared Rules"]
    end
    subgraph ACC1["Account A — Prod"]
        VPC1["VPC — associates<br/>shared rules"]
    end
    subgraph ACC2["Account B — Staging"]
        VPC2["VPC — associates<br/>shared rules"]
    end
    subgraph ONPREM["🏢 On-Premises"]
        DNS["Corp DNS Server"]
    end

    RULES --> SHARE --> VPC1
    SHARE --> VPC2
    IN <-->|"DX / VPN"| DNS
    OUT <-->|"DX / VPN"| DNS

    classDef hub fill:#4A90E2,stroke:#2E5C8A,color:#fff
    classDef acc fill:#7B68EE,stroke:#5A4BA0,color:#fff
    classDef onp fill:#E2A63F,stroke:#8A6A2E,color:#fff
    class IN,OUT,RULES hub
    class VPC1,VPC2 acc
    class DNS onp
```

> [!TIP]
> Create endpoints **once** in the Hub account. Share Resolver Rules via **AWS RAM** to all spoke accounts. No need to manage endpoints per-account.

---

## 📋 Summary

- **Direct Connect (DX)** — dedicated private physical cable to AWS; consistent latency, lower egress cost, no public internet
- DX connection types: **Dedicated** (your own port, 1G/10G/100G) vs **Hosted** (partner-shared, 50 Mbps–10G)
- Three VIF types: **Private VIF** (one VPC via VGW) | **Transit VIF** (many VPCs via TGW) | **Public VIF** (AWS public services)
- **DXGW (Direct Connect Gateway)** — global hub: one VIF → multiple regions (Private + Transit VIFs only)
- **DX is NOT encrypted by default** — add MACsec (L2, dedicated 10G/100G) or IPsec VPN (L3, any VIF)
- BGP failover: **Local Preference** controls on-prem→AWS path; **AS PATH Prepending** controls AWS→on-prem path
- **Route 53** — Hosted Zones (Public = internet-resolvable, Private = VPC-only), 7 routing policies, Alias records (free, apex-capable)
- **Hybrid DNS**: VPC built-in resolver at `VPC CIDR+2` can't be reached by on-prem natively
- **Inbound Endpoint** — on-prem DNS queries flow into AWS to resolve Private Hosted Zones
- **Outbound Endpoint + Forward Rules** — VPC queries for on-prem domains forwarded out to on-prem DNS servers
- Share Resolver Rules centrally via **AWS RAM** — one set of endpoints for the whole org

---

## 🔗 Connections (Zettelkasten)
- **Relates to:** [[1. VPC Deep Dive]], [[2.Transit Gateway|Transit Gateway]], [[VPC/VPN-connections|VPN Connections]]
- **Core Use Case:** Enterprise hybrid migration — databases stay on-prem during phase 1. EC2 app servers in AWS resolve `db.corp.internal` via Outbound Endpoint. On-prem clients resolve `api.internal` (now migrated to ECS) via Inbound Endpoint. All traffic flows over a Dedicated Connection for consistent latency.

---

## 🛠️ Study Aids

### 🧠 Mind Map

```mermaid
flowchart LR
    HYB((Hybrid Network<br/>& DNS))
    HYB --> DX["<b>Direct Connect</b><br/>Private physical wire<br/>Dedicated · Hosted<br/>Private · Transit · Public VIF"]
    HYB --> DXGW["<b>DX Gateway</b><br/>Global hub<br/>1 VIF → multi-region<br/>Not available for Public VIF"]
    HYB --> R53["<b>Route 53</b><br/>Public/Private zones<br/>Alias = free + apex<br/>7 routing policies"]
    HYB --> EP["<b>Resolver Endpoints</b><br/>Inbound: On-prem → AWS<br/>Outbound: AWS → On-prem<br/>Rules shared via RAM"]

    classDef root fill:#4A90E2,stroke:#2E5C8A,color:#fff,stroke-width:2px
    classDef node fill:#7B68EE,stroke:#5A4BA0,color:#fff
    class HYB root
    class DX,DXGW,R53,EP node
```

### 🗂️ Flashcards

#flashcards

**What is AWS Direct Connect and what is the main reason to use it over a VPN?**
?
Direct Connect is a dedicated private physical cable between your data center and AWS. The main reason to prefer it over VPN: consistent, guaranteed network performance (no internet congestion), lower egress costs, and higher bandwidth ceilings (up to 100 Gbps).

---

**Name the 3 VIF types in Direct Connect and what each one connects to.**
?
- **Private VIF** → Virtual Private Gateway (VGW) → private IPs in ONE VPC.
- **Transit VIF** → Transit Gateway (TGW) → private IPs across MANY VPCs.
- **Public VIF** → AWS public service endpoints (S3, DynamoDB, public Route 53) — no internet.

---

**Is Direct Connect encrypted? How do you add encryption?**
?
No, DX is private but NOT encrypted. Add encryption with:
1. **MACsec** (Layer 2 — hardware level, dedicated 10G/100G links only)
2. **IPsec VPN** (Layer 3 — run a Site-to-Site VPN tunnel *over* the DX link)

---

**What problem does a Direct Connect Gateway (DXGW) solve?**
?
A plain VIF only reaches resources in one region. DXGW acts as a global hub — a single Private or Transit VIF connects to DXGW, which then routes to VPCs/TGWs across multiple AWS Regions (except China).

---

**In Route 53, why prefer an Alias record over a CNAME for pointing to an AWS ALB?**
?
Two reasons:
1. **Alias works at the zone apex** (e.g., `example.com`). CNAMEs are forbidden at the zone apex.
2. **Alias queries are free**. CNAMEs are billed per DNS query.

---

**What Route 53 routing policy would you use for a gradual canary release (10% new / 90% old)?**
?
**Weighted routing policy** — assign weight 10 to the new version record and weight 90 to the old version record.

---

**In one sentence each, what is a Resolver Inbound Endpoint vs. an Outbound Endpoint?**
?
- **Inbound** — lets on-premises servers send DNS queries *into* AWS to resolve Private Hosted Zone records.
- **Outbound** — lets EC2 instances send DNS queries *out* of AWS to on-prem DNS servers to resolve corporate domain names.

---

**What is a Route 53 Resolver Forward Rule and how does it work?**
?
A Forward Rule maps a domain (e.g., `corp.internal`) to one or more on-prem DNS server IPs. When the VPC resolver receives a query matching that domain, it forwards the query through the Outbound Endpoint to those on-prem servers instead of resolving it locally.

---

**How do you scale Hybrid DNS across 20 AWS accounts without creating separate endpoints in each?**
?
Create the Inbound and Outbound Endpoints once in a central networking account, then share the Resolver Rules to all other accounts using **AWS Resource Access Manager (RAM)**. Each spoke account associates the shared rules with its VPCs.
